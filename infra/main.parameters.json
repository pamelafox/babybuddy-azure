{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
      "name": {
        "value": "${AZURE_ENV_NAME}"
      },
      "location": {
        "value": "${AZURE_LOCATION}"
      },
      "homeIP": {
        "value": "${HOME_IP_ADDRESS}"
      },
      "databasePassword": {
        "value": "${DATABASE_PASSWORD}"
      }
    }
  }
